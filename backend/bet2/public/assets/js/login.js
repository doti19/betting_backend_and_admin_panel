const login=async(email,password)=>{try{const res=await axios({method:'POST',url:'/geez/api/v1/users/login',data:{email:email,password:password}});if(res.data.status==='success'){console.log(res.data.user);showAlert('success','Logged in Successfully');window.setTimeout(()=>{location.assign('/geez/admin');},1000);}}catch(err){showAlert('danger',err.response.data.message);}}
document.querySelector('.user').addEventListener('submit',e=>{e.preventDefault();const email=document.getElementById('inputEmail').value;const password=document.getElementById('inputPassword').value;login(email,password);});