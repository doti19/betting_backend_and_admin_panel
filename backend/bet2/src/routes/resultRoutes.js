const express=require("express");const router=express.Router();const matchResult=require("../controllers/resultController");const authController=require("../controllers/authController");router.post("/",authController.protect,authController.restrictTo("superadmin","admin","dev_mgmt"),matchResult.registerMatchResult);router.get("/",matchResult.getAllMatchResults);router.get('/results/:leagueId',matchResult.matchResults);router.get("/:id",matchResult.getSingleMatchResult);router.patch("/:id",authController.protect,authController.restrictTo("superadmin","admin","dev_mgmt"),matchResult.updateMatchResult);router.delete("/:id",authController.protect,authController.restrictTo("superadmin","admin","dev_mgmt"),matchResult.deleteMatchResult);module.exports=router;